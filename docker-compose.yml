services:
  outpost:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - STATIC_DIR=/app/frontend/build
      - DB_PATH=/app/data/outpost.db
    volumes:
      - outpost_data:/app/data
      - C:\Users\avbir\Downloads\media:/media
    restart: unless-stopped

volumes:
  outpost_data:
